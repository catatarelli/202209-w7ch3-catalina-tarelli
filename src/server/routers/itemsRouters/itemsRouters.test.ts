describe("Given a GET /items/list endpoint", () => {
  describe("When it receives a request from user 'panchito'", () => {
    test("Then it should call the response method status with a 200, and a list of items owned by Panchito", () => {});
  });
});
